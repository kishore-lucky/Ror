<%= CSV.generate_line(["name","due_date","Status","user_no","created_at","updated_at"])%>
<% @tasks.each do |task|%>
<%= CSV.generate_line([task.name,task.due_date,task.Status,task.user_no,task.created_at,task.updated_at])%>
<% end%>